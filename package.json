{
  "name": "primary-newsflow",
  "version": "1.0.0",
  "description": "一级信息流 — 四大板块(AI情报/中国要闻/全球一级流/实时导航)聚合权威原始发布",
  "type": "module",
  "scripts": {
    "build": "NODE_USE_ENV_PROXY=1 node build.mjs",
    "serve": "node serve.mjs",
    "dev": "npm run build && npm run serve",
    "test": "node --test"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
